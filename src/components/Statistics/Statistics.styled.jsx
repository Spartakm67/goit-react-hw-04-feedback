import styled from '@emotion/styled'

const StatisticsStyle = styled.div`
   display: flex;
   flex-direction: column;
   gap: 10px
`;

const StatSpan = styled.span`
   font-weight: bold;
   font-size: 18px;
`;

export { StatisticsStyle, StatSpan };